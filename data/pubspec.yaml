name: data

environment:
  sdk: ^3.4.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain

  locator:
    path: ../locator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0